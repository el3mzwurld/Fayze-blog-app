# Fayze blog app — Using API : api.oluwasetemi.dev

A blog application built with **Vue** + **Vue Router 4**, using the real `https://api.oluwasetemi.dev/posts/` API.

## API Details

| Endpoint                     | Description         |
| ---------------------------- | ------------------- |
| `GET /posts?page=1&limit=12` | Paginated post list |
| `GET /posts/:uuid`           | Single post by UUID |

**Response shape:**



```bash
npm install
npm run dev
```
